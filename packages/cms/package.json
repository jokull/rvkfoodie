{
  "name": "@rvkfoodie/cms",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler d1 migrations apply rvkfoodie-agent-cms --remote && wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply rvkfoodie-agent-cms --local",
    "db:migrate:remote": "wrangler d1 migrations apply rvkfoodie-agent-cms --remote"
  },
  "dependencies": {
    "agent-cms": "file:../../../agent-cms"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
